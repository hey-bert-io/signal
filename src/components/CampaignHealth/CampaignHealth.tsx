import './CampaignHealth.css'
import { Avatar } from '../Avatar'; import { Card } from '../Card'; import { IconButton } from '../IconButton'; import { Progress } from '../Progress'; import { Status } from '../Status'; import { StatusIndicator } from '../StatusIndicator'; import type { CampaignHealthProps } from './CampaignHealth.types'
export function CampaignHealth({blocked,className,completed,deadline,deadlineDetail,inProgress,metrics,onMore,owner,progress,status,...props}:CampaignHealthProps){return <Card as="section" {...props} className={['signal-campaign-health',className].filter(Boolean).join(' ')}><div className="signal-campaign-health__summary"><div className="signal-campaign-health__header"><h2>Campaign health</h2><IconButton aria-label="More campaign health actions" icon={<span>⋯</span>} onClick={onMore} size="small" variant="ghost" /></div><Status tone="success">{status}</Status><div className="signal-campaign-health__indicators"><StatusIndicator label={`${completed} completed`} tone="success"/><StatusIndicator label={`${inProgress} in progress`} tone="info"/><StatusIndicator label={`${blocked} blocked`} tone="danger"/></div><Progress value={progress}/><div className="signal-campaign-health__owner"><h3>Owner</h3><div><Avatar initials={owner.initials} size={24} src={owner.avatarSrc}/><span>{owner.name}</span></div></div><div className="signal-campaign-health__deadline"><h3>Deadline</h3><time>{deadline}</time><span>{deadlineDetail}</span></div></div><div className="signal-campaign-health__metrics">{metrics.map(m=><div className="signal-campaign-health__metric" data-tone={m.tone} key={m.label}><span>{m.label}</span><strong>{m.value}</strong><span>{m.detail}</span></div>)}</div></Card>}
