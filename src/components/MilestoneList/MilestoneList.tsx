import './MilestoneList.css'
import { Card } from '../Card'; import { StatusIndicator } from '../StatusIndicator'; import type { MilestoneListProps,MilestoneRowProps } from './MilestoneList.types'
export function MilestoneRow({className,milestone,...props}:MilestoneRowProps){return <li {...props} className={['signal-milestone-row',className].filter(Boolean).join(' ')}><span className="signal-milestone-row__milestone"><StatusIndicator aria-hidden="true" showLabel={false} tone={milestone.state==='completed'?'success':'neutral'} /><span>{milestone.title}<span className="signal-milestone-row__state"> ({milestone.state})</span></span></span><time>{milestone.date}</time></li>}
export function MilestoneList({className,milestones,title='Milestones',...props}:MilestoneListProps){return <Card as="section" {...props} className={['signal-milestone-list',className].filter(Boolean).join(' ')}><h2>{title}</h2><ol>{milestones.map((m)=><MilestoneRow key={`${m.date}:${m.title}`} milestone={m} />)}</ol></Card>}
